import { render } from '@testing-library/react'

test('useNames work with only params', async () => {

//   const items = await screen.findAllByText(/Item #[0-9]: /)
//   expect(items).toHaveLength(10)
    expect(2+2).toEqual(4);
});